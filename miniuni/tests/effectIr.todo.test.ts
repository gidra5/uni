import { describe, it } from 'vitest';

describe('ts effect IR parity TODOs', () => {
  it.todo('normalizes effect syntax into algebraic-like IR nodes');
  it.todo('preserves bytecode under AST -> IR -> bytecode lowering');
  it.todo('compiles and executes through source -> IR -> bytecode -> interpret');
});
